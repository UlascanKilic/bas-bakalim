<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ulas_.deneme2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unicorn"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OyunModuNormal" />
        <activity android:name=".Kategoriler" />
        <activity
            android:name=".OyunSonu"
            android:theme="@style/AppTheme.OyunSonuTheme" />
        <activity android:name=".OyunModuCehennem" />
        <activity android:name=".TemaSec" />
        <activity android:name=".KayitOl" />
        <activity android:name=".Siralama"></activity>
    </application>

</manifest>